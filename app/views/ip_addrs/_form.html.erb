<%= form_for @ip_addr, html: {class: "form-horizontal"} do |f| %>
  <% if @ip_addr.errors.any? %>
    <div class="form-group">
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(@ip_addr.errors.count, "error") %> prohibited this ip_addr from being saved:</h2>
      <ul>
      <% @ip_addr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :description, class: "col-md-2 control-label" %>
    <div class="col-md-10">
      <%= f.text_field :description, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-md-2"></div>
    <div class="col-md-10">
      <%= f.submit "Update IP", class: "btn btn-primary" %>
      <%= link_to "Cancel", @subnet, class: "btn btn-danger" %>
    </div>
  </div>
<% end %>
